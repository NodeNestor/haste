[package]
name = "swift"
version = "0.4.2"
edition = "2021"
description = "swift - hyper-light fleet manager for haste agents: persistent and one-shot agents fed by drop-file inboxes."
license-file = "../LICENSE"
repository = "https://github.com/NodeNestor/haste"

[dependencies]
haste = { path = ".." }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
crossterm = "0.28"
